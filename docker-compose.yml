services:
  gradle:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace
      - gradle-cache:/root/.gradle
    environment:
      GRADLE_USER_HOME: /root/.gradle

volumes:
  gradle-cache: